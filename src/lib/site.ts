// Public link to the repository. Set NEXT_PUBLIC_SOURCE_URL to surface the
// "View source" affordances; when unset they're hidden rather than left as
// dead "#" links.
export const SOURCE_URL = process.env.NEXT_PUBLIC_SOURCE_URL;
